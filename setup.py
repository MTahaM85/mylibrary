from setuptools import setup, find_packages

setup(
    name="mylibrary",
    version="1.1.0",
    author="MTahaM",
    description="A simple library managment system.",
    packages=find_packages()
)